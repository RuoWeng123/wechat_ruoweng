<view class="container">
	<view class="flex-wrp" wx:for="{{unitList}}">
		<cover-view class="flex-item">{{item.label}}
			<!-- <cover-image src="{{item.path}}"></cover-image> -->
		</cover-view>
	</view>
</view>
<!-- <view wx:else bindtap="bindTapAddress">
  <button class="button" type='primary'>填写收货地址</button>
</view> -->
